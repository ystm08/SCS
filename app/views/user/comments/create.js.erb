$('#review-comments').html("<%= j(render "user/comments/show", review: @review) %>");
$('#comment_count').html("<%= @review.comments.count %>")
$('#comment_textfield').val('');
if ($("#comments-error").length>0) {
  $("#comments-error").empty();
}