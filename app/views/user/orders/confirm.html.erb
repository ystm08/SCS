<div class="container">

  <div class="row my-5">
    <div class="mx-auto">
      <h3><strong>Confirm order</strong></h3>
    </div>
  </div>

  <div class="row">
    <div class="mx-auto">
      <table class="table">
        <thead>
          <tr>
            <th>商品名</th>
            <th>単価</th>
            <th>数量</th>
            <th>小計</th>
          </tr>
        </thead>
        <tbody>
          <% @cart_items.each do |cart_item| %>
          <tr>
            <td><%= cart_item.item.name %></td>
            <td><%= number_with_delimiter(cart_item.item.add_tax_price, delimiter: ',') %>円</td>
            <td><%= cart_item.amount %></td>
            <td><%= number_with_delimiter(cart_item.subtotal, delimiter: ',') %>円</td>
          </tr>
          <% end %>
          <tr>
            <td></td>
            <td></td>
            <td>請求金額：</td>
            <td><%= @cart_items_price %>円(税込)</td>
          </tr>
        </tbody>
      </table>
    </div>
  </div>

  <div class="row mt-5 mb-3 ml-5">
    送料は無料です。<br>
    お支払いは代金引換えのみになります。<br>
  </div>
  <div class="row mt-4 ml-5">
    <table>
      <tr>
        <td>お届け先&emsp;</td>
        <td>〒<%= current_user.post_code %></td>
      </tr>
      <tr>
        <td></td>
        <td><%= current_user.address %></td>
      </tr>
      <tr>
        <td></td>
        <td><%= current_user.name %></td>
      </tr>
    </table>
  </div>

  <div class="row mt-5">
    <div class="col-7"></div>
    <div class="col-2">
      <div class="d-flex justify-content-end">
      <%= link_to "戻る", cart_items_path, class: "custom-link2" %>
      </div>
    </div>
    <div class="col-2">
      <%= form_with url: orders_path, method: :post do %>
      <%= submit_tag "注文を確定する", class: "custom-submit" %>
      <% end %>
    </div>
  </div>

</div>