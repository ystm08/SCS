<!--@item → item-->
<div><%= item.name %></div>

編集ボタンif文

<div><%= image_tag url_for(item.item_image.variant(resize: "400x400")) %></div>
<div><%= item.introduction %></div>
<div>カテゴリー:<%= item.category.name %></div>
<div>¥<%= number_with_delimiter(item.add_tax_price, delimiter: ',') %>(税込)</div>

個数
いいね保存
カートに入れるボタン　if文