<% if obj.errors.any? %>
  <div class="row mt-3">
    <div class="mx-auto">
      <%= obj.errors.count %>件のエラーが発生しました
    </div>
  </div>
  <div class="row mt-3">
    <div class="mx-auto">
      <ul>
        <% obj.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  </div>
<% end %>